# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py37

[testenv]
deps =
    -r tests/requirements.txt
    -r requirements.txt
commands =
    coverage run --source=dbt_cloud_api_client --branch -m unittest discover
    coverage report -m
    codecov -t ${{ secrets.CODCOV }}
